import FooterNav from "./FooterNav";

export default function Footer() {
  return (
    <footer className="fixed inset-x-0 bottom-0 z-header mx-auto w-full max-w-[600px] bg-black">
      <FooterNav />
    </footer>
  );
}
